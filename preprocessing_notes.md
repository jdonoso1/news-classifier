# Preprocessing Notes

Steps applied:
1. Lowercase
2. Remove non-alphabetic characters (punctuation, numbers)
3. Normalize whitespace

Tried both with and without stopwords — keeping them with TF-IDF
worked fine since TF-IDF penalizes high-frequency terms anyway.
